[build-system]
    requires = ["setuptools>=64", "setuptools-scm"]
    build-backend = "setuptools.build_meta"

[project]
    name = "esp-idf-defs"
    dynamic = ["version"]
    dependencies = []

[tool.setuptools]
    packages = ["esp_idf_defs"]

[tool.setuptools_scm]
